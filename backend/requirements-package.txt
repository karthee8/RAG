# Lightweight runtime dependencies for the PACKAGED desktop backend.
# Excludes torch / sentence-transformers (replaced by ONNX fastembed),
# heavy langchain meta-packages (only the small text-splitter is used),
# Postgres driver (app uses bundled SQLite), and dev/test tooling.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0
sqlalchemy==2.0.30
redis==5.0.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
langchain-text-splitters==0.2.1
numpy==1.26.4  # chromadb 0.5.0 uses np.float_, removed in NumPy 2.0 — must stay <2
chromadb==0.5.0
fastembed==0.8.0
rank-bm25==0.2.2
pypdf==4.2.0
python-docx==1.1.2
httpx==0.27.0
python-dotenv==1.0.1
structlog==24.2.0
